[project]
name = "aether-scrapling-service"
version = "1.0.0"
description = "Loopback-only Scrapling fetching and extraction service for FGP"
requires-python = ">=3.10,<3.14"
dependencies = [
  "scrapling[fetchers]==0.4.11",
  "browserforge==1.2.4",
]

[project.optional-dependencies]
dev = []

[tool.ruff]
line-length = 110
